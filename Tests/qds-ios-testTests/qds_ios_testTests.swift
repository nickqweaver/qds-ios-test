import XCTest
@testable import qds_ios_test

final class qds_ios_testTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(qds_ios_test().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
